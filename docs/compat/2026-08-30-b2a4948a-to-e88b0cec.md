# Compatibility report — spec re-pin `b2a4948a…` → `e88b0cec…` (2026-08-30)

> Interim report, written by hand from the re-pin evidence. The layered generator
> (`scripts/compat-report.py` / `scripts/compat-snapshot.sh`) regenerates this file mechanically; the
> classification below is what those layers produce.

**Source contract:** `b2a4948af3…` (1,426,283 B) → `e88b0cec03…` (1,426,472 B), 101 → 101 operations.

**Upstream diff (additive):** a new `cosine` object property (`{ type: object, additionalProperties: {} }`)
on the `usage.source` inline schema, new `cosine` / `Cosine` values on several enums, and one enum entry
(`category`) re-ordered.

## Layers

| Layer | Signal | Result |
| --- | --- | --- |
| OpenAPI source | byte diff = additive (`cosine` property + enum values; one reorder); no operations added or removed | **minor** |
| Semantic | the additive `cosine` property re-derived the `MessagesResult.usage.source` audited allOf digest; re-audited in the overlay; `apiImpact` non-breaking | **minor** |
| Kotlin source | snapshot `048c58c8…` → `a8341cc4…`; 8 generated files gained the additive `cosine` handling; **no files removed**; 1850 files / 1849 `.kt` unchanged | **minor** |
| JVM ABI | `sdk/api/sdk.api` + `sdk/api/jvm/sdk.api`: **+55 / −6** each. Additive: new `$Cosine` open-enum subclasses (`ByokProviderSlug`, `InlineByokGetParameter…`, `InlineMessagesStartEvent…`, `InlineProviderResponse…`) + `getCosine`/`setCosine` accessors. The **−6 lines are generated data-class all-args constructor signature changes** (`ProviderOptions`, `InlineVideoGenerationRequestProviderOptions…`, `InlineImageGenerationProviderPreferencesOptions…` gained a `cosine` `Map` field) — **zero real API removals**, but a changed constructor is a `−` line | **breaking** |
| klib ABI | `sdk/api/sdk.klib.api` — **no change** | **patch** |
| Wire + behaviour | JVM tests + golden/contract suites pass on the regenerated tree | **patch** |
| Targets | klib target set unchanged | **patch** |

## Release classification: **breaking** (label `compat:breaking`)

The upstream change is additive, but under the strict JVM-ABI rule (any `−` line is breaking) it classifies as
**breaking**: several generated data classes gained a `cosine` field, changing their all-args `<init>`
constructor signatures. There are **no real API removals** — every removed line is a superseded generated
constructor, and the corresponding `$Builder` gained an additive `setCosine`. So the break affects only code
that calls a generated model's all-args constructor directly; **Builder-based and deserialization-based usage is
additive**, and open enums decode the new `cosine` / `Cosine` values as `SdkUnknown`. Pre-1.0, this is permitted
with the `compat:breaking` label and this rationale (see `docs/compatibility-policy.md` §0.x).

## Spec-maintenance note (called out per the re-pin procedure)

The additive `cosine` property landed on the audited `MessagesResult.usage.source` allOf branch, so the
`allof-resolution-audit` overlay's pinned `inlineSchemaSha256` for that branch was re-derived from the
generator's own "matched 0" diagnostic (`c5634ea6…` → `f86b9036…`), `info.version` bumped to `1.2.0`, and the
overlay's own digest re-synced in `spec/pin.json` and `spec/sdkgen.yaml`. This is a **spec-input maintenance
edit, not a source edit** — no `.kt`/`.kts` file was hand-edited. `zeroMatchPolicy`/`conflictPolicy` remained
`fail`.
