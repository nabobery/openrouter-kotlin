# Binary-compatibility-validator ABI dumps are tool-generated (`./gradlew :sdk:apiDump`) and must be committed
# byte-for-byte as the tool emits them — including its trailing blank line at EOF. Exempt them from git's whitespace
# checks so that canonical output is not flagged (hand-"fixing" it would fail `apiCheck`).
sdk/api/**/*.api -whitespace
